function League(title, year, teams) {
  this.title = title;
  this.year = year;
  this.teams = teams;
}

League.prototype.hasEnded = function() {
  // if all teams played a match against each other in the same division
}
